# UI 自查与补充文档

- 文档版本：`v1.0`
- 编写日期：`2026-03-21`
- 适用对象：`你自己 / 前端实现 / 后续补 Figma`

## 1. 当前 UI 页面数量判断

按你目前的描述，当前应该已有 6 个 UI 目标：

1. 工作台首页
2. 检测报告页
3. 改写对比台
4. 模型配置页
5. 上传论文交互弹窗
6. 工作台首页空状态

## 2. 我实际获取到的 Figma 内容

我真正从 Figma 中成功读取并确认过的，是下面 4 个：

1. 工作台首页
2. 检测报告页
3. 改写对比台
4. 模型配置页

后面你补的这 2 个，我没有真正从 Figma 读取成功：

1. 上传论文交互弹窗
2. 工作台首页空状态

原因不是你没做，而是当时 Figma MCP 和 Figma API 都碰到了调用额度或限流问题，所以我只能依据你的口头说明和前面文档做实现判断，没有拿到它们的真实布局细节。

结论：

- 你当前 UI 目标数：`6`
- 我真正从 Figma 成功获取到的：`4`
- 我没有拿到实际设计稿、只能推断实现的：`2`

## 3. 当前代码实现自查

## 3.1 已有的页面或组件

当前代码里已经有这些页面或 UI 入口：

- [WorkspacePage.tsx](D:\projects by ai_coding\论文降重工具（1）\frontend\src\pages\WorkspacePage.tsx)
- [DocumentDetailPage.tsx](D:\projects by ai_coding\论文降重工具（1）\frontend\src\pages\DocumentDetailPage.tsx)
- [ReportPage.tsx](D:\projects by ai_coding\论文降重工具（1）\frontend\src\pages\ReportPage.tsx)
- [RewritePage.tsx](D:\projects by ai_coding\论文降重工具（1）\frontend\src\pages\RewritePage.tsx)
- [ModelsPage.tsx](D:\projects by ai_coding\论文降重工具（1）\frontend\src\pages\ModelsPage.tsx)
- [UploadDialog.tsx](D:\projects by ai_coding\论文降重工具（1）\frontend\src\components\UploadDialog.tsx)

## 3.2 覆盖情况判断

### 工作台首页

实现情况：`部分完成`

已覆盖：

- 欢迎区
- 数据统计卡
- 最近论文列表
- 快捷操作区
- 当前活跃任务卡
- 历史任务表
- 空状态

仍不完整：

- 没有完全还原你 Figma 中更精细的视觉层级
- 没有检测任务 / 改写任务切换 Tab
- 缺少更接近设计稿的快捷操作图标卡片样式
- 缺少首页错误提示态
- 当前导出按钮里还写死了 `localhost:3000`，应该改成统一 API 地址

### 检测报告页

实现情况：`部分完成`

已覆盖：

- 顶部分数摘要
- 风险段列表
- 段落详情侧栏
- 去改写入口
- 检测中状态
- 无结果空状态

仍不完整：

- 现在是“段落卡片列表”，不是你 Figma 里那种“正文阅读区直接高亮风险段”的效果
- 缺少更贴近设计稿的右侧报告摘要布局
- 缺少 PDF、检测耗时、上一段/下一段风险导航
- 缺少检测失败态

### 改写对比台

实现情况：`部分完成`

已覆盖：

- 原文 / 改写双栏
- 改写前后分值
- 采纳
- 回退
- 再生成
- 复检
- 处理进度

仍不完整：

- 没有完全还原你 Figma 里的顶部信息栏和策略栏
- 缺少多候选版本切换区
- 缺少更明显的文本差异高亮
- 缺少手动微调区域
- 缺少版本保存入口

### 模型配置页

实现情况：`部分完成`

已覆盖：

- 模型列表
- 基础参数表单
- 测试连接
- 默认模型设置
- 删除模型
- 资源估算说明

仍不完整：

- 没完全还原你 Figma 里的即时预览测试区布局
- 缺少真正的测试输入区和结果输出区
- 缺少滑杆、开关等更贴近设计稿的控件表现
- 缺少更强的“本地模型 / 云端模型”分组表达

### 上传论文弹窗

实现情况：`基础版，未按你新 Figma 细化`

已覆盖：

- 论文标题
- 语言
- 文件选择
- 上传进度
- 失败提示

仍不完整：

- 我没有读到你做好的 `75% 进度` 弹窗设计，所以当前只是通用上传弹窗
- 缺少拖拽上传视觉区
- 缺少“上传完成，解析中”的单独状态
- 缺少更接近设计稿的阶段文案
- 缺少上传完成后的视觉过渡态

### 工作台首页空状态

实现情况：`有，但只是通用空状态`

已覆盖：

- 无论文时的首页空状态
- 主按钮引导上传

仍不完整：

- 没有按你最新 Figma 空状态样式做专门还原
- 没有更细的插图、引导文案和品牌化表达

## 4. 真实结论

如果严格按“你现在的 Figma 已有 6 个 UI 目标”来评估：

- `真正比较贴近的`：工作台首页主结构、检测报告主结构、改写对比主结构、模型配置主结构
- `只做了可运行骨架，还没贴近你后补设计的`：上传弹窗、首页空状态
- `整体完成度`：更接近 `功能骨架已通`，还不是 `高还原 UI 实现`

也就是说，你觉得“实现得不全”，这个判断是对的。

问题不在于主流程没做，而在于：

- 我优先把“能运行的页面和接口”搭起来了
- 但还没有把所有页面做成和你当前 Figma 一样的细化版本
- 尤其是后补的状态页和弹窗页，我没有拿到真实设计稿，只能做通用实现

## 5. 当前最需要补的 UI 项

如果下一步继续补 UI，优先级建议如下：

1. 上传论文弹窗的完整状态版
2. 工作台首页空状态的真实还原版
3. 检测报告页改成“正文高亮 + 右侧风险详情”版
4. 改写对比台补多候选切换与差异高亮
5. 模型配置页补即时测试输入和结果区

## 6. 当前最需要补的代码项

除了 UI 还原本身，代码里还建议补这些：

1. 工作台首页任务表增加 `检测任务 / 改写任务` 切换
2. 工作台首页导出按钮改掉写死的 `localhost:3000`
3. 上传弹窗增加“上传完成，正在解析”状态
4. 检测报告页增加失败态
5. 改写页增加多候选版本切换
6. 模型配置页增加即时测试输入区

## 7. 适合你下一步继续补 Figma 的清单

如果你继续画 Figma，我建议优先补或确认这些节点：

1. 上传论文弹窗完整状态
2. 首页空状态
3. 检测报告失败态
4. 改写页多候选态
5. 模型配置测试成功 / 失败态

## 8. 适合我下一步继续补代码的清单

如果你要我继续实现，我建议下一轮优先做：

1. 按你已有 Figma 把上传弹窗细化
2. 把首页空状态细化成你设计稿样式
3. 重做检测报告正文高亮区
4. 补改写页候选切换和差异高亮
5. 修复工作台导出按钮地址
